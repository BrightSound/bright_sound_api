source 'https://rubygems.org'

gem 'bundler'
gem 'grape'
gem 'json'
gem 'sequel' # ORM
gem 'jwt' # used for authentication token

group :development, :test do
  gem 'rake'
  gem 'sqlite3'
  gem 'dotenv'
end

group :test do
  gem 'rack-test'
  gem 'rspec'
end
